# ================================================================
# 日常配置 — 非敏感信息，打包后以明文形式随应用分发
# 敏感逻辑（加密算法等）请放在 config/secret/ 目录
# ================================================================

# ── 应用信息 ──────────────────────────────────────────────────────
NAME    = "Desktop App"   # 窗口标题 & 托盘提示
VERSION = "1.0.0"
ICON    = "icon.png"      # assets/ 目录下的图标文件名（支持 .ico / .png）

# ── 窗口 ──────────────────────────────────────────────────────────
WINDOW_WIDTH      = 1280
WINDOW_HEIGHT     = 800
WINDOW_MIN_WIDTH  = 800
WINDOW_MIN_HEIGHT = 600
WINDOW_RESIZABLE  = True
CLOSE_TO_TRAY     = True  # True = 关闭按钮隐藏到托盘；False = 直接退出

# ── 开发服务器 ────────────────────────────────────────────────────
DEV_PORT  = 5174   # Vite 开发服务器端口
VITE_WAIT = 2.5    # 等待 Vite 就绪的秒数

# ── 日志 ─────────────────────────────────────────────────────────
LOG_LEVEL        = "INFO"  # DEBUG / INFO / WARNING / ERROR / CRITICAL
LOG_MAX_SIZE_MB  = 5       # 单文件最大 MB
LOG_BACKUP_COUNT = 3       # 保留历史日志份数
